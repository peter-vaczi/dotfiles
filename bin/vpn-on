#!/bin/bash

DEAFULT_ROUTE=`ip route | grep default`

echo "start vpnc..."
sudo vpnc --username vaczi || exit 1



echo "setup routes..."
sudo ip route del default
sudo ip route add 10.0.0.0/8 dev tun0 scope link
sudo ip route add $DEFAULT_ROUTE

ip route

